"""vlabs-sdk — SDK contracts for Verifiable Labs (mirror subset).

This distribution ships the SDK-safe subset of the ``verifiable_labs_envs``
package: run configuration, the provider interface + dummy provider, the
typed schemas, and the formal-spec math mirror. Do NOT install alongside
the full ``verifiable-labs-envs`` package — they share the import name;
this repo becomes the canonical home at split-flip time.
"""

__version__ = "0.0.1"
